name: Test all (on all branches)

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: dotnet build
        run: dotnet build

      - name: dotnet test
        run: dotnet test
